import React from "react";
import ReactDOM from "react-dom";

const App = () => {
  return <div>Hello from React and TypeScript Application!</div>;
};

ReactDOM.render(<App />, document.getElementById("root"));
